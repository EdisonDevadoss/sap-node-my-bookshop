{
  "name": "my-bookshop",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "ISC",
  "dependencies": {
    "@sap/cds": "$USED_VERSION",
    "express": "$USED_VERSION"
  },
  "engines": {
    "node": "^8.9"
  },
  "scripts": {
    "build": "cds build/all --clean",
    "deploy": "cds deploy",
    "start": "cds run"
  },
  "devDependencies": {
    "sqlite3": "^4.1.0"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db/",
          "srv/"
        ],
        "credentials": {
          "database": "db/my-bookshop.db"
        }
      }
    }
  }
}